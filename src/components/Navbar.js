import React from 'react';
import { StyledNavbar } from '../styled/Navbar';

const Navbar = () => {
  return (
    <StyledNavbar>
      <h1>DevJobs</h1>
    </StyledNavbar>
  );
};

export default Navbar;
